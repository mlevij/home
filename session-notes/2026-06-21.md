# Discovery Farms — Embed Sizing/Zoom Reconciliation, Section Reorder — Session Log

---

## Session: 2026-06-21

Short session, mostly iterating on `index.html` embed sizing/ordering from 2026-06-18's work.

### Files touched
- `repos/DF/index.html`
- `repos/DF/drought-monitor.html`
- `repos/DF/sentinel-ratio-monitor.html`

---

## Iframe sizing vs. Leaflet zoom — two separate knobs

User reported the embedded maps (drought monitor, Sentinel) were too tall for a smaller monitor. Scaled down the CSS:
- `.map-embed` 600px → 480px
- `.map-embed-tall` 1100px → 720px

This alone didn't fix the "boundaries cut off" look — **shrinking the container doesn't change how much geographic area is visible at a fixed Leaflet zoom level**. At a constant zoom, fewer vertical pixels just shows less ground area, re-introducing the original cutoff problem from the other direction. Fix: reduced the actual `setView()` zoom argument from `8.5` → `7.5` in both `drought-monitor.html` and `sentinel-ratio-monitor.html` to compensate — zooming out a notch so the full intended area fits in the smaller container.

**The exact line changed each time**: `}).setView([38.1, -108.0], 7.5);` — second argument is zoom level.

## Direct GitHub web-edit conflict

While this was in progress, user edited `drought-monitor.html` directly on GitHub's web UI (changed `zoomSnap` from `0.5` to `0.35`, across two commits), then realized that wasn't the right fix and asked to finish the zoom update locally. Had to `git fetch`/inspect the diff, stash local changes, pull the web-edit commits, then reapply: reverted `zoomSnap` back to `0.5`, applied the zoom `8.5`→`7.5` fix on top, for both `drought-monitor.html` and `sentinel-ratio-monitor.html`. Pushed cleanly, no lost work either side.

## Caching confusion ("the zoom always comes out the same")

User repeatedly saw no visible change after pushes. Diagnosed: this is most likely **iframe sub-resource caching** specifically — `index.html` embeds `drought-monitor.html`/`sentinel-ratio-monitor.html` via `<iframe src="...">`, and a hard-refresh of the parent page doesn't reliably force a fresh fetch of the nested iframe document in all browsers. Confirmed via direct `curl` against the live GitHub Pages URL each time that the *server* was serving the correct updated value — the mismatch was client-side. Recommended testing by opening the iframed page directly in its own tab; offered a cache-busting query string (`?v=2` etc. on the iframe `src`) as a future mitigation if it keeps recurring, not yet implemented.

## Section reorder

Per user's preferred narrative flow (local/regional → wide), reordered `index.html`'s embedded sections from `Drought → GRACE CONUS → GRACE Basin → Sentinel` to **`Drought → Sentinel (veg/soil) → GRACE Basin → GRACE CONUS`**.

---

## Pushed this session
- Scaled embed heights (`801c243`)
- zoomSnap revert + zoom 8.5→7.5 fix (`95ff742`)
- Section reorder (`123fd9c`)

All live at `https://mlevij.github.io/DF/`.

---

## Pending (carried over from 2026-06-18, unchanged)
See `2026-06-18.md`'s "Pending / next session" list — GEE script update/rewrite for fresh CSV-driven charts, more charts once CSVs downloaded, Sentinel yearly composites (pinned), basin monthly/yearly GRACE aggregates, `assets/grace_colorado/` + `recolor_boundary_lines.py` cleanup decision, unused Landsat/Sentinel-2 GIF assets, and renaming `render_grace_frames_colorado.py` (it's the basin script now). None of these were touched this session.

---

## Session: 2026-06-21 (cont'd) — GEE CSV charts, header rewrite, GLDAS surface/root-zone/GWS exports

### Files touched
- `repos/DF/index.html`
- `repos/DF/scripts/plot_gee_timeseries.R` (new)
- `repos/DF/scripts/test_sentinel1_amplitude_trend.R` (new, ad hoc — not meant to be a permanent asset)
- `repos/DF/gee/soil_moisture_acquisition.js`
- `repos/DF/assets/sentinel1_vh_vv_anomaly_swco.png` (generated)
- `repos/DF/assets/grace_fo_tws_anomaly_swco.png` (generated)

### Header blurb + per-map descriptions
Rewrote `index.html`'s header intro — dropped the old "from 1984 to present" framing (relic from the previous DF page; only the PDSI chart actually goes back that far) in favor of summarizing the three live maps (USDM, Sentinel-1, GRACE) with a pointer down to the trend-chart section. Also wrote/updated the `section-body` description under each of the four map embeds (Drought Monitor, Sentinel-1, GRACE Basin, GRACE CONUS) — user supplied most of the language, reused largely verbatim.

### New R charting pipeline (first time R has been used in this repo — everything else in `scripts/` is Python)
Built `scripts/plot_gee_timeseries.R` to chart the two GEE CSV exports (`sentinel1_swco_timeseries.csv`, `grace_fo_swco_timeseries.csv`) in the same red/blue dry-wet bar style as the existing `pdsi_timeseries_swco.png`. Key decisions, all confirmed with user before building:
- **Separate-story framing, not a forced comparison** — Sentinel-1 (vegetation-density anomaly) and GRACE (raw TWS anomaly) get their own charts rather than being normalized onto the PDSI/USDM drought scale (apples-to-oranges since PDSI/USDM are already composite indices, GRACE/Sentinel-1 are raw physical measurements).
- **GRACE chart uses the raw mascon TWS anomaly as-is** (already baseline-relative from the GEE script, no reprocessing into percentile space) — kept deliberately distinct from the modeled GRACE-DA percentile product used by the live `grace-monitor.html`/`grace-basin-monitor.html` maps. The two GRACE "products" in this project are NOT the same pipeline; see `project_df_storymap` memory.
- **Monthly resolution, not annual** — both datasets are ~8 years (2018–2025); annual averaging would leave too few points and erase the seasonal signal.
- Charts added to `index.html`'s "Drought Trends" chart-stack (now 4 charts, intro line updated to match) with their own descriptions.
- Path gotcha: R's working directory defaults to `Documents`, not the script's folder — switched `assets_dir` to an absolute path (`C:/Users/mlevij/repos/DF/assets`) rather than relying on a relative `../assets`.

### Sentinel-1 "trending denser" — tested, did not hold up
User noticed the Sentinel-1 chart looked like it was trending toward denser vegetation over time. Tested via `test_sentinel1_amplitude_trend.R` (linear regression of mean anomaly, summer peak, winter trough, and peak-minus-trough amplitude, each vs. year): none significant (p = 0.17–0.78, R² ≤ 0.29). Conclusion: visual impression is likely noise from a short 8-season record plus one standout 2023 peak, not a real trend. Decided NOT to report this as a finding on the site.

### Surface / root-zone / groundwater — raw GEE layers added to the .js script
User asked whether to add surface/root-zone/groundwater soil moisture breakdowns alongside the GRACE TWS chart. Resolved:
- The already-downloaded GRACE-DA NetCDFs (`GRACE_NC4/`, 1230 cached weekly files) contain ONLY percentile variables (verified via `nc_open` → `gws_inst`, `rtzsm_inst`, `sfsm_inst` — no raw value band), so "recompute raw from the percentile files" isn't possible — a percentile can't be inverted back to a physical value.
- Instead added raw GEE-derived layers to `gee/soil_moisture_acquisition.js`: broke the existing GLDAS NOAH compositing into `gldas_surface` (0–10cm) and `gldas_rootzone` (0–10/40/100cm sum) bands alongside the existing `gldas_tws` total, all as anomalies vs. the same 2004–2009 baseline as GRACE — so all three share one cm-LWE axis. Added three new `Export.table.toDrive` CSV tasks: `gldas_surface_swco_timeseries`, `gldas_rootzone_swco_timeseries`, `grace_gws_swco_timeseries` (the GLDAS-corrected groundwater variant — distinct from the live map's `gws_inst` percentile toggle).
- Naming asymmetry explained to user: `gldas_*` = pure GLDAS model output (no satellite gravity involved); `grace_gws` = GRACE satellite TWS measurement minus the GLDAS model correction term — GRACE is the primary source there, GLDAS is just the correction. Also clarified there is no purely-satellite-measured, depth-resolved soil moisture product — surface/root-zone is inherently model output (GLDAS), not a direct retrieval, unlike Sentinel-1's actual radar backscatter.
- Planned: once these 3 CSVs land, build a 3-line companion chart (surface/root-zone/groundwater anomaly over time) to sit next to the existing TWS chart, telling a "response speed by depth" story (surface fast/jagged, groundwater slow/smooth). Also need to add a line to the GRACE chart caption explaining the 2004–2009 baseline (NASA's fixed mascon-product reference period, chosen pre-2010s Colorado River Basin megadroughts — not a rolling/30-yr normal).

### Performance bug found and fixed in the .js script (mid-session)
User reported the 3 new CSV export tasks (surface/root-zone/GWS) were taking an extremely long time to run in GEE — much longer than the original 2 CSVs. Root-caused to two separate anti-patterns introduced while adding the GLDAS layers:
1. `graceGWS` did `monthlyGldas.filter(ee.Filter.eq('year_month', ym)).first()` **inside** a `.map()` over every GRACE image — forces EE to fully re-evaluate `monthlyGldas`'s entire month-by-month construction once per GRACE image (~96×96 recomputation). Fixed with a proper `ee.Join.saveFirst` (year_month match) between `graceAnomalies` and `monthlyGldas` instead.
2. `monthlyGldas` itself was built by filtering the ~23,000-image 3-hourly GLDAS-over-CONUS collection by a `year_month` **string-equality** property, once per each of ~96 months — can't use EE's time index, forces a near-full scan per month. Fixed by switching to `filterDate(monthStart, monthEnd)` per month (parsed from the `ym` string via `ee.Date.parse`), which EE can index directly. This affects all three new CSV exports, not just GWS.

**Status at end of session:** user kicked off the re-run with both fixes in place; still slow-ish, user expects it may need to run overnight. Next session: check whether the 3 CSVs (`gldas_surface_swco_timeseries.csv`, `gldas_rootzone_swco_timeseries.csv`, `grace_gws_swco_timeseries.csv`) have landed in `repos/DF/assets/`, inspect their structure, build the 3-line chart, add the GRACE 2004–2009 baseline explanation to chart captions, and remove/decide fate of the ad hoc `test_sentinel1_amplitude_trend.R` script (not part of the permanent pipeline).

---

## Session: 2026-06-21 (cont'd #2) — 3-line depth chart, LWE/baseline explainers, basin/CONUS fix, commit + push

Picked back up after the overnight GEE re-run finished. All 3 new CSVs (`gldas_surface_swco_timeseries.csv`, `gldas_rootzone_swco_timeseries.csv`, `grace_gws_swco_timeseries.csv`) landed successfully in `repos/DF/assets/`.

### Files touched
- `repos/DF/scripts/plot_gee_timeseries.R`
- `repos/DF/index.html`
- New: `repos/DF/session-notes/` (copied/extracted from this AES folder, see below)
- Committed/pushed: `de53dbc`

### Three-line depth-profile chart
Added to `plot_gee_timeseries.R`: reads the 3 new CSVs, builds `soil_water_profile_anomaly_swco.png` — surface/root-zone/groundwater anomaly as three colored lines (light→dark blue = shallow→deep) on one shared cm-LWE axis, line chart rather than bars since the story is relative response speed/magnitude by depth, not a per-month wet/dry call. Added as a 5th chart in `index.html`'s Drought Trends section.

**User caught a real misread on first look**: the near-flat line in the new chart is surface, and user initially expected groundwater (the "slow" layer) to look flattest. Correct explanation isn't response *speed*, it's storage *capacity* — a 10cm soil layer has a hard physical ceiling on how much its anomaly can ever swing, while groundwater integrates deficits/surpluses over a much larger reservoir with no such ceiling, so it can drift 20+ cm and stay there. Original session framing ("surface fast/jagged, groundwater slow/smooth") undersold this and was edited in the chart caption + `index.html` description to lead with capacity, not speed.

### LWE explainer added
User asked "what is LWE again?" — added a plain-language explainer to the GRACE TWS chart description in `index.html`: GRACE measures gravity changes (mass shifting in/out of a region), converted to "liquid water equivalent" — the depth in cm that missing/extra mass would form if spread evenly as a layer of water. Also clarified in the depth-profile description that "anomaly" means departure from baseline, not total water present — first glance read it as absolute storage amount.

### Baseline explainer added
Per the plan from earlier in the session, added the 2004–2009 GRACE baseline explanation to both the GRACE TWS and depth-profile chart captions (R script) and `index.html` descriptions: it's NASA's fixed mascon-product reference period (not a rolling 30-yr normal), and it happens to predate the 2010s–2020s Colorado River Basin megadroughts, so current deficits are measured against a period that wasn't itself unusually wet or dry.

### Basin/CONUS description order fix
User caught that the Colorado River Basin map description still said "here that same dataset is zoomed..." as if it came after the CONUS map — leftover from the 2026-06-21 (first session, see above) section reorder that put Basin *before* CONUS. Fixed: Basin description now stands alone as the primary GRACE intro; CONUS description now says "here that same dataset is zoomed *out*... fits into the bigger picture" referencing Basin correctly as coming first.

### Session-notes folder created in DF repo
User asked to copy relevant AES session-notes entries into a new `repos/DF/session-notes/` folder. Copied wholesale (pure DF content): `2026-03-19.md`, `2026-06-17.md`, `2026-06-18.md`, `2026-06-21.md`. Extracted DF-only sections from two mixed AES+DF files: `2026-06-08.md`, `2026-06-15.md` (each new file notes it was extracted, since the AES originals also cover unrelated AES Soil Profiles work). Skipped files with no DF content or only a one-line passing mention. **Note:** this DF copy was a one-time snapshot as of that point in the session — it will drift from this AES file unless deliberately kept in sync going forward.

### Commit + push (`de53dbc`)
User wanted only today's actual work live on GitHub, not the large pile of pre-existing untracked local files sitting in the repo (~1230 cached `assets/grace/*_2002...` PNGs, `grace_monthly/`, `grace_yearly/`, GIFs, `README.md`, `discovery_farms_drought_2012/2020.html`, other `scripts/*.py`, `.Rhistory`). Confirmed scope with user via explicit question before staging. Committed/pushed only: `index.html`, `gee/soil_moisture_acquisition.js`, `scripts/plot_gee_timeseries.R`, the new `session-notes/` folder, and the 5 new CSVs + 3 new chart PNGs in `assets/`. Everything else remains untracked, deliberately.

### Pending / next session
- `scripts/test_sentinel1_amplitude_trend.R` — ad hoc, not part of the permanent pipeline, still sitting untracked. Decide whether to delete or keep.
- The large untracked file pile (cached GRACE PNGs, GIFs, README, other scripts) was deliberately left out of this commit — no decision made yet on whether/how to handle it long-term.
- `repos/DF/session-notes/` will need a manual re-sync if more DF-relevant entries get added here in `repos/AES/session-notes/` going forward (no automated mirroring set up).
- Carried-over pending items from 2026-06-18 (GEE script rewrite scope, Sentinel yearly composites, basin monthly/yearly GRACE aggregates, `assets/grace_colorado/` cleanup decision, unused Landsat/Sentinel-2 GIFs, `render_grace_frames_colorado.py` rename) — still untouched.
