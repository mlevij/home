# ECRC/GVRC Duplicate-Mukey Sweep and AllDepth Field Source Investigation — Session Log

---

## Session: 2026-07-15

Two threads, both diagnosis-only (no writes to the gdb or any published service): (1) checking whether the PRC duplicate-mukey bug from `2026-07-14.md` (Part 4) also affects other stations, and (2) continuing the Part 11 investigation into where the live site's missing `_DCP_0_6_in`/`_AllDepth` fields originally came from.

### Files touched
- Read-only only: queried `Master List.gdb` directly via arcpy (using ArcGIS Pro's bundled Python env at `C:\Users\mlevij\AppData\Local\Programs\ArcGIS\Pro\bin\Python\envs\arcgispro-py3\python.exe`) and inspected `CO_SSURGO.gpkg` via GDAL/osgeo from a separate local Python 3.10 install. No edits made anywhere.
- Wrote today's session note (this file).

---

## Part 1: ECRC and GVRC have the same duplicate-mukey bug PRC had

User noticed clicking a map unit that spans two polygons in ECRC only highlights one polygon at a time (not both) — same symptom as the PRC/WIA bug fixed last session. Then flagged GVRC doing the same thing.

Ran a full read-only sweep of all 10 stations' `_Official_unproj`/`_Official_26913` row counts and `_Soils` mukey duplicates via direct arcpy `SearchCursor` queries (not relying on Pro's UI). Confirmed:

| Station | `_Official_unproj` | `_Soils` duplicate mukeys |
|---|---|---|
| ARDEC | 1 row, partCount=3 (correctly dissolved multipart) | none |
| ARDEC South | not found under `ARDEC_South_Official_unproj`/`_26913` (worth checking actual naming next session) | none |
| AVRC | `_unproj` missing, but `_26913` exists as 1 row | none |
| **ECRC** | **2 rows, undissolved (partCount=1 each)** | **94987, 95003, 95004, 95035 (×2 each)** |
| **GVRC** | **2 rows, undissolved (partCount=1 each)**, `_26913` not built yet | **506779, 506793, 506809 (×2 each)** |
| OMRC | 1 row | none |
| PRC | 1 row, partCount=2 — confirmed still fixed from 2026-07-14 | none |
| RMRC | 1 row | none |
| SLVRC | 1 row | none |
| SWCRC | 1 row, `_26913` not built yet | none |

**Root cause confirmed identical to the PRC bug:** `ECRC_Official_unproj` and `GVRC_Official_unproj` were never dissolved into a single multipart feature (still 2 separate single-part rows each), so `Intersect` in `Clip Soils to Updated Boundaries.ipynb` produces one output row per boundary row for any mukey overlapping both — same mechanism as the WIA/PRC case, just never fixed for these two stations because PRC was the one rebuilt from scratch this cycle.

**Fix (not yet applied — diagnosis only, user had to step away before approving):**
1. Dissolve `ECRC_Official_unproj` and `GVRC_Official_unproj` down to one row each (merge parcels into a single multipart feature, matching every other station's pattern).
2. Delete stale `ECRC_Official_26913` and let cell-0 of `Batch Project Layers.ipynb` regenerate it from the dissolved source; `GVRC_Official_26913` will simply get created fresh (doesn't exist yet).
3. Re-run `Clip Soils to Updated Boundaries.ipynb` for both stations.
4. Re-check `ECRC_Soils`/`GVRC_Soils` for zero duplicate mukeys, same as was done for PRC.

Everything else (ARDEC, ARDEC South, AVRC, OMRC, RMRC, SLVRC, SWCRC) is clean — no duplicate mukeys anywhere else.

---

## Part 2: AllDepth field source — ruled out `muaggatt`, still unresolved

Continuing from `2026-07-14.md` Part 11 (live site regression: `_DCP_0_6_in`/`_AllDepth` fields missing after the PRC-rebuild Overwrite).

Located `CO_SSURGO.gpkg` at `C:\Users\mlevij\Documents\ArcGIS\Projects\Master List\Resources\SSURGO Portal\CO_SSURGO_gpkg\CO_SSURGO.gpkg` (2.2 GB, 103 layers) and inspected it directly via GDAL (`osgeo.ogr`) rather than opening it in Pro.

**Good news — all 18 `_DCP_0_6_in`/taxonomy `_DCP` fields from the Part 11 missing-fields list are pre-baked** as 26 `rating_*` layers in this gpkg (`rating_Sand_DCP_0_6_in`, `rating_TaxClName_DCP`, `rating_EcoSiteNm_DCP`, etc.), one row per mapunit (7435 rows each, matching the `mapunit` table's row count). These are Soil Data Viewer exports keyed on **`areasymbol` + `musym`** (composite key) — not `mukey` — so joining them back onto `mupolygon`/`_Soils` will need that composite join, not a direct `mukey` join.

**Initially guessed `muaggatt.drclassdcd`** as the source for the `DrainageCl` field — user corrected this: doesn't recall `muaggatt` being used at all, and believes the `_AllDepth` (whole-profile) fields were computed **server-side**, not sourced from a precomputed ArcGIS/gpkg table. This is more consistent with the data anyway: searched every layer/field name in `CO_SSURGO.gpkg` for `alldepth` and found only `component.MaxEC_AllDepths`/`MaxSAR_AllDepths`/`MinpH_AllDepths` — different fields (min/max extremes, not weighted averages) that don't match the needed `Sand_AllDepth`/`Silt_AllDepth`/`OM_AllDepth`/etc. **No `_AllDepth` field matching the needed names exists anywhere in this gpkg.**

**Not yet resolved — next step:** find the actual server-side computation. Checked `build_all_profiles.R` and `AES_soil_profiles/` (R pipeline) and the ArcGIS notebooks folder for `AllDepth`/depth-weighting logic (`hzdept_r`/`hzdepb_r`/`comppct_r`) — nothing found yet in those locations. Likely lives somewhere not yet located (a separate script, or logic that ran once historically and was never checked into `AES_soil_profiles` or the ArcGIS notebooks). `DrainageCl`'s source is also still unconfirmed — `muaggatt.drclassdcd` was a guess the user doesn't think is right; needs a fresh look, not a re-guess.

---

## Aside: system slowness traced to wifi, not a stuck process

User reported ArcGIS Pro running very slowly and Spotify barely working today. Checked CPU load (31%), free memory (15.4 GB free of 31.3 GB), and top processes by CPU/memory — nothing pegged or runaway (ArcGISPro's CPU number was cumulative lifetime seconds, not a live spike). Ping to 8.8.8.8 showed an initial 116ms then dropped to sub-20ms. User switched from 5GHz to 2.4GHz wifi and confirmed things got smoother — consistent with 5GHz interference/congestion, not a local resource problem. Not a codebase issue, just noting in case the same symptom recurs.

---

## Part 3: Soil Health Map (`soil/index.html`) — fractional water content project kicked off

Separate thread, unrelated to the AES/ArcGIS pipeline above — pivoted to `soil/index.html` (the CoAgMET & RAWS Soil Health Monitoring map, a different site from `AES/index.html`/`aes-data.html`). User's running notes call for a "fractional water content" map layer, restored Saxton-Rawls output, a precip overlay, and CF/Inorganic C — planned in full via plan mode; plan saved at `C:\Users\mlevij\.claude\plans\transient-launching-fox.md` (5 tasks, one at a time: full data resync → depth table UI → pipeline extension → water content layer → precip overlay).

**Task 1 (done):** `soil/index.html`'s embedded per-station `depths` data (CoAgMET 41 stations/123 rows, RAWS 18 stations/54 rows — counts match the `2026-07-14.md` Part 1 verification exactly) was fully stale placeholder data, never updated after that session's %OM/WP/FC fix. Regenerated the entire dataset directly from `Soil Characteristics Data Sheet.xlsx` (read-only extraction via `openpyxl`) — sand/silt/clay/pH/EC/%C/%N/C:N/WP/Saxton-Rawls FC/Sat/Bagnall FC all replaced, plus new `caco3`/`inorgC` fields (both networks) and `cf` (RAWS only). One gotcha caught before it shipped: the RAWS sheet's column literally labeled "CF" is raw coarse-fragment mass in grams, not a percentage — the actual displayable percentage is the neighboring "ϑg CF" column; fixed before generating the final data. Station name sets matched 1:1 between the old hardcoded data and the spreadsheet (verified via diff) — no additions/removals needed, just value corrections. Verified via `git diff --stat` that only the two data blocks changed.

**Task 2 (done):** Added `%CaCO3`, `%Inorg C`, and `CF %` columns to the on-page depth table (header colspans and row template in `soil/index.html`), reusing the existing `fmt()` helper (already renders `—` for null/undefined, so CoAgMET's `cf` needed no explicit null-fill).

Verified both tasks by serving the repo root locally (`python -m http.server`) and checking the live page, since `/soil/...` fetches are absolute-path and break under `file://`.

**Noted, not fixing now:** RAWS's live "Live Soil Moisture" chart (the 7/30/90-day span selector) hits NIFC's `dataNesdisidRange` endpoint directly from the browser on every span change, with no caching — user noticed it's slow, especially at 90 days. This is a different code path from the Task 3 pipeline work (which only pulls a ~2-day window for a single latest-reading snapshot), so Task 3 won't speed this up. Leaving as-is per user; would need a separate caching/precompute pass for the historical chart itself if it's ever worth fixing.

**Task 3 (done):** Added `.github/scripts/fetch_coagmet_daily.py` (bulk CoAgMET `data/daily.json` precip fetch, validates every station ID against `coagmet-metadata.json` first so one bad ID can't 400 the whole request) and `.github/scripts/fetch_raws_snapshot.py` (resolves each of the 18 lab-sampled RAWS stations to its live NESS ID, pulls latest VWC at 2"/8"/20" plus a recent precip delta from cumulative `RNIN`). Wired both into `update-soil-moisture.yml`, using `python3` explicitly (no `setup-python` step on the runner, so bare `python` isn't guaranteed to resolve). Both tested live and their output committed once (`soil/coagmet-daily.json`, `soil/raws-snapshot.json`) so the site isn't waiting on the first scheduled run.

**Found and fixed along the way:** the spreadsheet's CoAgMET station `HYK01` doesn't match any real CoAgMET station ID — the only Holyoke-area station is `hyk02`. User confirmed by hand it was a typo and fixed it directly in the spreadsheet; renamed the corresponding key everywhere on the code side too (`COAGMET_SOIL['HYK01']` → `'HYK02'` in `soil/index.html`, and the pipeline script's station ID list). A brace/bracket balance check on the full inline script (no headless browser available in this environment to actually load the page — no `chromium-cli`/Node/Playwright/Selenium installed) turned up a second, related bug: `COAGMET_NETWORK`'s full station list had a **stray duplicate `HYK01` entry** appended out of alphabetical order at the end of the array (the real, correctly-ordered `HYK02` entry already existed at its proper place) — would have drawn two overlapping markers for the same station once renamed. Removed the duplicate.

**Task 4 (done):** Added the "Fractional Water Content" layer — `waterLayer`, sidebar toggle (`btn-water`) with a Dry→Wet gradient legend, and `loadWaterContent()`. Computes `(live VWC% − lab WP) / (lab FC − WP)` clamped to [0,1] per station, using the dataviz skill's default sequential blue ramp (light=dry, dark=wet) since this site has no existing brand palette to substitute in — **could not run the skill's `validate_palette.js` locally (no Node runtime)**, so used the documented ramp as-is rather than re-validating. Only plots stations with both a lab sample and a live sensor reading (CoAgMET: just `oth01` today: RAWS: ~17 of 18, `PINION CANYON` has no active soil sensor). Depth-matching rule: shallowest sensor reading (CoAgMET 4", RAWS 2") against each station's shallowest lab band.

Gave the water layer its own Leaflet pane (`waterPane`, z-index 650) after user noticed it rendering underneath RAWS points — root cause was a real race condition, not just draw order: `loadRAWS()` and `loadWaterContent()` both fire async fetches around the same time, so which one's markers land in the DOM first (and thus stack on top) wasn't guaranteed. The pane makes the stacking deterministic regardless of fetch timing.

**Verification limits this session:** no headless browser tooling is available in this environment (checked for `chromium-cli`, Node/Playwright, Python `selenium`/`playwright` — none installed), so Tasks 3/4 were verified via direct script runs against the live APIs + static syntax/reference checks, not an actual rendered-page screenshot. Accidentally launched a real (non-headless) Edge window once while probing for a browser binary via `--version` — closed with no lingering process, user confirmed no harm done. Static site was served locally (`python -m http.server`) and the user checked it in their own browser instead.

**Task 5 design clarified:** user originally imagined the precip overlay as a heatmap; on rereading, the plan as written was actually point markers (same pattern as the water layer). Flagged the difference (~59 sparse stations statewide would make a true interpolated heatmap visually overclaim precision the data doesn't support) — user confirmed **point markers**, matching the water layer's pattern.

**Task 5 (done):** built as point markers, proportional-symbol (marker radius = 7-day total, one fixed hue) rather than a second sequential color ramp next to the water layer's blue one — same reasoning as the water layer (no Node runtime here to run the dataviz skill's validator on a new palette). Also added a matching **Precipitation chart** (bar, not line) to the per-station panel, respecting the same 7/30/90-day selector as VWC/Temp: CoAgMET's daily total used directly, RAWS's derived from differencing consecutive cumulative `RNIN` readings (resets clamped to 0), SNOTEL's own daily `PRCP` field, CO Soil Health shows "no rain gauge" (confirmed via its actual JSON schema, not assumed).

**Live-chart split (done):** the old combined VWC+Temp dual-axis chart was split into two independent single-axis charts (dual-axis is a known anti-pattern — the two scales visually skew how the lines compare). The 7/30/90-day "Time period" selector was also moved to sit once, directly under the depth table, instead of being sandwiched between the Snowpack and Soil Moisture charts.

**Metadata parity (done):** RAWS already had elevation/county/reporting-since wired up natively (pre-existing, not new); found and fixed a real bug where the new water/precip markers passed `null` for RAWS meta instead of reusing it. Added SNOTEL's unused `beginDate` field as "reporting since." CoAgMET and CO Soil Health have no native county field at all — added a one-time FCC Area API lookup (`fetch_station_counties.py`, NOT part of the daily Action, station coordinates are static) covering all 126 CoAgMET stations + 2 CO Soil Health sites, zero failures.

**SNOTEL Saxton-Rawls data (done):** populated WP/FC/Sat/CF for 47 SNOTEL stations from `soil/SNOTEL-data-for-Levi.xlsx`'s "SNOTEL-Saxton-Rawls" sheet, keyed by `stationId` (clean numeric match against AWDB's own field). Wired into the existing depth-table/chart rendering via `showSite()`. One station excluded: "Cottonwood Pass" (sheet ID 1281) is actually SNOTEL-Lite (`SNTLT`), not regular SNOTEL (`SNTL`), so it was never in this site's station list. Caught a related bug: the depth table's Texture cell used raw `${d.tex}` instead of `fmt(d.tex)` — harmless until now, would've printed the literal word "undefined" for every SNOTEL row since this sheet has no texture data.

**Methodology section (done, this iteration):** rebuilt as a nested accordion matching `AES/aes-data.html`'s "Maps & description" pattern (outer show/hide toggle + independently-collapsible sub-topics), attached directly under the map card (`rounded-b-none`/`-mt-4 rounded-t-none`, same technique as that reference file) instead of floating as a separate card. Copy trimmed down, the metadata sub-section removed, "Zentra" purged from user-visible text (confirmed remaining internal mentions are only code identifiers). Water Content and Precip toggles now default OFF (button class and initial layer-group attachment both changed).

**Not done — real gap still open:** the Fractional Water Content layer doesn't cover SNOTEL yet, even though it now has lab WP/FC. It also needs a live VWC reading, and SNOTEL currently only fetches live data per-station on click — no precomputed snapshot like RAWS/CoAgMET have. Would need a new pipeline step mirroring `fetch_raws_snapshot.py`.

**Data-quality observation, not a bug:** several SNOTEL stations (Berthoud Summit, Grizzly Peak, High Lonesome, Jones Pass, Lost Dog, Sawtooth, Slumgullion, Zirkel) share byte-identical WP/FC/Sat/CF values across all depths in the source spreadsheet — looks like a fallback default soil profile for sites without site-specific modeling. Not an extraction error on this end; flagging in case it wasn't expected.

## Part 4: Methodology tab — iterative visual polish (done, this session)

Took several rounds to land on the "AES/aes-data.html"-style folder-tab look the user wanted:
- Renamed "How these numbers are calculated" → "Methodology"; removed the metadata sub-section and trimmed the copy per user request; dropped the "Zentra" name from all user-visible text (confirmed only internal identifiers remain, e.g. `NETWORK_BADGE_STYLE.Zentra`).
- **Real bug found and fixed along the way**: the initial `-mt-4`/`space-y-5` negative-margin technique (copied from `aes-data.html`) was fighting Tailwind's `space-y-5` sibling-selector rule, which has higher CSS specificity than a plain utility class — risked losing that fight unpredictably. Fixed properly by wrapping the map card + methodology tab together in a plain `<div>` with no `space-y` class, so they stack with true zero default margin between them, no cascade ambiguity.
- **Root-caused an apparent "still not updating" report**: it wasn't a browser cache issue (ruled out via incognito + a direct `curl` fetch confirming the server was serving the correct file) — the real cause was that the toggle button lived *after* the collapsible content in the DOM, so when collapsed (0 height), the card's own `p-5` padding plus the button wrapper's own spacing stacked into ~40px of blank space that read as "a gap to the map." Moved the button to the top of the card, content expands below it.
- Iterated the tab's width (now spans full width minus ~3px each side, not `max-w-md`), padding (down to near-hairline: `pl-px pr-px pb-px pt-1` on the outer card, `pt-1` specifically a bit larger to clear the map's drop-shadow bleed), and the toggle button itself (explicit `w-[calc(100%-8px)] mx-auto` — plain `w-full` + margin doesn't work correctly on a flex container, it overflows; centered explicit width is the robust fix), plus `text-sm` sizing and reduced height (`py-1`).
- Matched the FWC/Precip sub-accordion item widths (`w-[calc(100%-8px)] mx-auto`) and explanation-text size (`text-xs`, one step below the `text-sm` titles) to the same button for visual consistency.
- Moved the Snowpack chart to the bottom of the SNOTEL chart stack (was first; now VWC → Temp → Precip → Snowpack) — pure DOM reorder, nothing else depends on section order.

## Part 5: "Most SNOTEL stations aren't working" — investigated, likely not a bug

User reported most SNOTEL stations show no WP/FC/Sat/CF data when clicked. Re-verified thoroughly rather than assuming the earlier work was solid:
- Re-fetched the live AWDB station list fresh and re-checked all 47 `SNOTEL_SOIL` keys against it — all 47 IDs still match (a few station *names* have since drifted slightly, e.g. "Whiskey Creek" vs. live "Whiskey Ck", but the join is by numeric `stationId`, not name, so this doesn't affect matching).
- Checked the `SNOTEL_SOIL` block itself for malformed data (`NaN`/`None`/`undefined`) — none found.
- Confirmed `showSnotelSite()`'s routing logic and `showSite()`'s `smsDepths` parameter threading are both still intact (untouched by the recent styling/reorder edits).
- Tested the exact unencoded AWDB query the production code uses (`stationTriplets=*:CO:SNTL&activeOnly=true`) 3x live — all returned 200 OK (a *different* encoding of this same query 500'd once earlier this session, but that looks like a one-off transient server hiccup, not a systemic issue with production's URL form).

**Leading theory, not yet confirmed with the user:** only 47 of the ~118 live CO SNOTEL stations have Saxton-Rawls data in the spreadsheet at all (~40% coverage) — the other ~71 correctly show "No lab soil sample available" when clicked, which is expected/correct behavior given the source data, not a malfunction. Asked the user which specific stations they tested to confirm this is what's actually happening (e.g. a station they know should be in the 47 still failing would point to a real bug) — **no answer received yet, pick this up first thing next session.**

## Part 6: SSURGO texture-sheet plausibility review (reported, not actioned)

User asked whether the *other* sheet in `SNOTEL-data-for-Levi.xlsx` ("SNOTEL-data from soil map" — texture/%clay/%sand/OM/BD/pH/EC per horizon, distinct from the WP/FC/Sat/CF sheet already wired in) looks like a sound SSURGO-based approximation. Ran a conservative check (only flagging things confidently wrong, plus clearly-borderline cases for the user's own judgment — deliberately did NOT trust a hand-reconstructed USDA texture-triangle classifier for anything ambiguous, given the risk of a subtly-wrong polygon implementation producing false positives):

- **Fairly clear-cut:** two Oi horizons (Cascade 2, Cochetopa Pass) at 85% organic matter still carry a mineral texture class ("loam") — texture classes describe the mineral fraction, so assigning one to a horizon that's 85% organic doesn't really make sense; reads like a leftover default value. A third Oi horizon (Schofield Pass) has only 4% OM, which is backwards — that low an OM reads more like a mineral A horizon that got mislabeled as organic.
- **Borderline, flagged not asserted:** a handful of Bt/BE horizons where clay/sand numbers sit right at a class boundary and arguably read closer to a different texture class than the one labeled (exact rows/values given to the user in-conversation, not reproduced here — see chat log if needed, or re-run `scratchpad` logic against the sheet again).
- **Physically odd but not impossible:** a couple of Bt (subsoil) horizons with unusually low bulk density (~0.71-0.76 g/cm³) — B horizons are usually denser than the A horizon above, not lighter; could be legitimate for andic/volcanic-influenced high-country soils, or could be a data issue.

This is purely about the texture/OM/pH sheet — does not affect the WP/FC/Sat/CF data already wired into the live site (that's from the separate "SNOTEL-Saxton-Rawls" sheet, unaffected). **Not actioned yet** — this was a review/sanity-check only; no decision made on whether/how to correct anything in the source spreadsheet (which the user edits manually, per their standing preference).

---

## Pending / next session
- **Resolve the "most SNOTEL stations not working" question** — get specific station names the user tested, to confirm whether this is just the expected ~40% coverage ratio (not a bug) or a real remaining issue.
- **Decide what to do with the SSURGO texture-sheet findings** (Part 6) — Oi horizons with mineral texture classes, one likely horizon mislabel, a few borderline texture-vs-composition calls, a couple of low subsoil bulk-density values. User edits this spreadsheet manually; needs their call on whether any of this warrants a correction.
- **Apply the ECRC/GVRC fix** once approved: dissolve both `_Official_unproj` boundaries, regenerate `_26913`, re-run Clip Soils, verify zero duplicate mukeys (Part 1 above).
- **Find the actual `_AllDepth` computation source** — not in `CO_SSURGO.gpkg`, not (per user) `muaggatt`, not yet found in `build_all_profiles.R`/`AES_soil_profiles`. User believes it was computed server-side; needs locating before the live site's All Depths section can be restored.
- Re-confirm `DrainageCl`'s actual source (the `muaggatt.drclassdcd` guess was rejected).
- Check `ARDEC South`'s actual boundary layer naming in the gdb — sweep above didn't find it under the expected `ARDEC_South_Official_unproj`/`_26913` names, though `ARDEC_South_Soils` itself is present and clean.
- Carried over from `2026-07-14.md`: restore full DCP/AllDepth field set to the published `_Soils_WM` layers (now partially unblocked — DCP half has a confirmed source in `CO_SSURGO.gpkg`'s `rating_*` layers); check boundary layers for leftover assessor fields; verify GVRC/OMRC/SWCRC hosted projection (26912 vs 26913); re-apply/decide on symbology; spot-check live site once fixes land.
- RAWS live-chart slowness (span selector hits NIFC live with no caching) — known, separate, unfixed issue, left as-is per user.
- **Fractional Water Content layer needs a SNOTEL precompute pipeline** before it can include SNOTEL stations, now that they have lab WP/FC (see Part 5 note above — no live VWC snapshot exists for SNOTEL yet, only per-click fetch).
- Consider whether SNOTEL-Lite (`SNTLT`) stations (e.g. Cottonwood Pass) should be added to the SNOTEL station list at all — currently entirely excluded since the query only pulls `SNTL`.
- Once satisfied with the soil map changes, remember these are all still uncommitted local changes (`soil/index.html`, `.github/workflows/update-soil-moisture.yml`, `.github/scripts/`, the new `soil/*.json` snapshot files) — commit when ready, per user's "we can commit it all later."
