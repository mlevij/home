# AES Soil Profiles — Session Log

---

## Session: 2026-06-15

### Files touched
- `repos/AES/AES/aes-data.html`
- `repos/AES/AES/all_stations_profiles.json` (read-only, not regenerated)

---

## Profile display: OSD-first with SSURGO fallback

### Decision
Reversed the previous SSURGO-primary approach. Now displays OSD as the primary profile when available, SSURGO as fallback. Root cause for reversal: manual spot-checking against SoilWeb showed SSURGO profiles were inconsistent in many cases. **Note: SoilWeb actually displays SSURGO component data**, not OSD — the inconsistencies may have been from SSURGO entries with generic horizon names (H1, H2, H3) rather than real designations (A, Bt, C, etc.). This was not fully resolved.

### Current JS logic (`aes-data.html` ~line 642)
- **Both OSD + SSURGO available**: show OSD, with SSURGO accessible via a rotating cube toggle button
- **OSD only**: show OSD, no toggle
- **SSURGO only (no OSD)**: show "No OSD for this map unit. Click to see SSURGO data." button that reveals SSURGO inline
- **Neither**: "No profile data"

### Profile title
Dynamic heading above the profile: "OSD Soil Profile" / "SSURGO Soil Profile", updates on toggle.

---

## Cube rotation toggle animation

### Implementation
CSS 3D cube using `transform-style: preserve-3d` on the track div, two `position:absolute` face divs at `translateZ(55px)` and `rotateY(90deg) translateZ(55px)`.

Key settings:
- `perspective: 400px; perspective-origin: 50% 0%` on the outer container — origin at top prevents the profile being clipped at the top due to perspective scaling
- `overflow: hidden; height: 375px` on outer container — clips visual overflow at bottom
- `backface-visibility: hidden` on both faces — prevents see-through during rotation
- `background: white` on faces — makes the block look solid during rotation

Toggle button centered below profile: starts as "SSURGO ›", flips to "‹ OSD" and back. Uses the same chevron SVG style as the "More Data" button.

---

## Layout / sizing changes

### Main container (`aes-data.html` line 215)
- **Before**: `<main class="container mx-auto px-4 py-6">` — Tailwind `container` class capped content at 1536px max-width, leaving large margins at high resolutions (user's display: 2560×1600 at 200% scale)
- **After**: `<main class="px-6 py-6">` — full viewport width

### Popup width (`.esri-popup__main-container`)
- `min-width: 420px; max-width: 560px` — allows natural word wrapping while preventing collapse

---

## Pending issues

### musym 56 at ARDEC — boundary mismatch
- **mukey**: 497705 ("Kim-Thedalund loams, 3 to 15 percent slopes", CO644)
- **Root cause**: The SSURGO mupolygon for 497705 falls within ARDEC's bounding box but does NOT intersect the ARDEC boundary shapefile used in `build_all_profiles.R`. The ArcGIS feature service uses a slightly different boundary and shows it as part of ARDEC.
- **Component data exists**: dominant component is Kim (45%), Thedalund (35%). OSD available for Kim. SSURGO horizon names are generic (H1, H2) — same quality issue noted elsewhere.
- **Decision**: Do not apply a buffer fix — risk of capturing unintended polygons from neighboring survey areas.
- **Action needed**: Verify accuracy of ALL station boundary shapefiles before publishing to AES website. The ARDEC boundary may need to be updated in GIS to include this polygon. Flag to user before final push.

### Other still pending
- Full check of all stations for profile correctness (only ARDEC checked in detail)
- Push to GitHub when ready (personal review first, then AES handoff)

### Deferred to post-handoff
- **Mobile responsiveness** — app is desktop-first; main challenges are ESRI popup (needs vertical stacking layout at narrow widths, min-width must be removed), sidebar (needs hamburger/drawer), map height (700px too tall on phone), and touch target sizing. Estimated ~1 day of work. Not needed for initial AES handoff.

---

## Discovery Farms — GRACE Animation Fixes (continued from 2026-06-08)

### GEE script status
Script confirmed saved in repo at `repos/DF/gee/soil_moisture_acquisition.js`. Tasks had not completed from previous session.

### drought-monitor.html
Confirmed final and ready to embed. All 26 USDM images (2000–2025) present in `assets/drought_years/`. GitHub Pages hosting is free with no bandwidth limits.

### GRACE animation: 12×14 pixel output
First export came back at 12×14 pixels. Root cause: GEE ignored `dimensions:800` and exported at native GRACE resolution (~55km/pixel over the SW Colorado bbox = ~12×14 native pixels).

**Fix 1** — Added `resample('bicubic').reproject({crs:'EPSG:4326', scale:10000})` before `.visualize()` in each frame. Forces GEE to work at 10km resolution so `dimensions:800` produces a proper-sized video. Bicubic smooths the coarse GRACE pixels rather than leaving blocky squares.

**Fix 2** — Expanded region from `swColorado` to `fourCorners` (`[-115.0, 31.0, -101.5, 42.5]`) covering AZ, CO, NM, UT with margins. GRACE is too coarse (~5-6 pixels over SW Colorado) to show meaningful spatial variation at that scale; Four Corners gives ~30+ pixels and makes drought patterns legible.

**Fix 3** — Added TIGER/2018/States boundary lines baked into each frame:
```javascript
var stateBoundaries = ee.FeatureCollection('TIGER/2018/States');
var stateMask = ee.Image().byte()
  .paint({featureCollection: stateBoundaries, color: 1, width: 1});
// In graceFrames map:
return graceViz.where(stateMask, ee.Image.constant([255, 255, 255]).byte());
```
White 1px lines. Can bump to width:2 if hard to see against mid-palette neutrals.

Updated `grace.filterBounds` and `graceAnomalies.clip` to use `fourCorners`. GRACE CSV time series still reduces over `swColorado` geometry.

Export.video region changed to `fourCorners`; CSV exports unchanged.

### CRS for GEE task dialog
`EPSG:4326` (WGS84). Consistent with the `reproject()` call in the code; appropriate for video embeds in StoryMaps.

### Soil health site — planning only, no implementation
Features discussed for `mlevij.com/soil/`:
- CF and Inorg C — waiting on RAWS CSV from colleagues (same refresh as RAWS soil data)
- SNOTEL network layer — can start now, NRCS public API
- Precip overlay — can start now, needs source decision (PRISM/CoAgMET/gridMET)
- Fractional water content heatmap — blocked until more active stations (SNOTEL may help)
- Phase 4 (merge AES + soil health + CoAgMET link) — uncertain, not committed

Quench (`spatial.colostate.edu/quench/`) is the larger CSU colleague-managed tool. Soil health site independently evolved to similar functionality. NDVI/NDWI/NDMI discussed as alternatives for showing soil moisture change visually — viable but a separate GEE pipeline if pursued.

### Pending
- Resubmit `grace_fo_tws_swco_animation` task with updated script
- Sentinel-1 tasks still exporting (VV animation, ratio animation, both CSVs)
- Build `soil-moisture.html` and `grace.html` once MP4s land in Drive
